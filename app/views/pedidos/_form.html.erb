<%= form_with(model: pedido) do |form| %>
  <% if pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
        <% pedido.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <br>
<div class="container">
  <div class="field">
    <%= form.label :cliente_id %>
   <%= form.select :cliente_id, Cliente.all.collect { |cliente| [cliente.nome, cliente.id] }, 
    {:selected => @pedido.cliente_id}, {:class=> 'form-control'} %>
  </div>

  <div class="field">
    <%= form.label :valor_total %>
    <%= form.text_field :valor_total, class: "form-control" %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit 'Cadastrar '%>
  </div>
<% end %>
</div>
